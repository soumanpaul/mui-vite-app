import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#845218',
        },
        secondary: {
            main: '#164a25',
        },
    }
})

export default theme;